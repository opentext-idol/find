'use strict';

// Requires leaflet first (free global L, supplied by Leaflet's unconditional expose()).
require('leaflet');
module.exports = require('leaflet.markercluster/dist/leaflet.markercluster-src');
